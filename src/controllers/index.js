const controllerUser = require('./controllerUser');

const controllerLogin = require('./controllerLogin');

const controllerCategories = require('./controllerCategories');

module.exports = {
  controllerUser,
  controllerLogin,
  controllerCategories,
};